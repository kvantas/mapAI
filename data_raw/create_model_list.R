# The built-in PAI transformation model definitions used to live here and were
# compiled into the binary R/sysdata.rda via usethis::use_data(internal = TRUE).
#
# They now live in package source at R/models.R, so the fitting and prediction
# mathematics is reviewable in a diff and cannot drift from a generator script.
# Edit R/models.R directly; nothing needs to be regenerated.
