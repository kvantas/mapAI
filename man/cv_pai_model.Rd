% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assess_pai_model.R
\name{cv_pai_model}
\alias{cv_pai_model}
\title{Assess PAI Model Performance}
\usage{
cv_pai_model(
  gcp_data,
  pai_method,
  validation_type = "random",
  k_folds = 5,
  train_split_ratio = 0.8,
  n_strata = 4,
  seed = 123,
  ...
)
}
\arguments{
\item{gcp_data}{An \code{gcp} object of homologous points.}

\item{pai_method}{A character string or a custom model list for \code{train_pai_model}.}

\item{validation_type}{The validation strategy: "random", "spatial", "probability", or "stratified".}

\item{k_folds}{Number of folds for CV. Defaults to 5.}

\item{train_split_ratio}{Proportion of data for training (for "probability" and "stratified"). Defaults to 0.8.}

\item{n_strata}{Number of strata for stratified sampling. Defaults to 4.}

\item{seed}{An integer for reproducibility.}

\item{...}{Additional arguments passed to \code{train_pai_model}.}
}
\value{
An object of class \code{pai_assessment} containing a summary data frame,
detailed prediction results, and validation parameters.
}
\description{
Performs model validation to estimate a PAI model's predictive
performance using k-fold cross-validation or design-based probability
sampling.
}
\details{
Model validation is crucial for understanding how well a model will
generalize to new data. This function automates this process.

\strong{Validation Types}:
\itemize{
\item \strong{\code{random}}: Standard k-fold cross-validation.
\item \strong{\code{spatial}}: Spatial k-fold cross-validation using k-means clustering.
\item \strong{\code{probability}}: A single train/test split using simple random sampling.
\item \strong{\code{stratified}}: A single train/test split using stratified
random sampling based on the magnitude of distortion vectors.
}
}
\examples{
\dontrun{
# --- 1. create a demo data set
demo_data <- create_demo_data(seed = 1)
gcp_data <-demo_data$gcp

# --- 2. Assess with RANDOM k-fold CV ---
random_assessment <- cv_pai_model(
  gcp_data, pai_method = "lm", validation_type = "random", k_folds = 5
)
print(random_assessment)

# --- 3. Assess with SPATIAL k-fold CV ---
spatial_assessment <- cv_pai_model(
  gcp_data, pai_method = "lm",
  validation_type = "spatial",
  k_folds = 5
)
print(spatial_assessment)

# --- 4. Assess with PROBABILITY (simple random) sampling ---
prob_assessment <- cv_pai_model(
  gcp_data, pai_method = "lm",
  validation_type = "probability",
  train_split_ratio = 0.75
)
print(prob_assessment)

# --- 5. Assess with STRATIFIED probability sampling ---
stratified_assessment <- cv_pai_model(
  gcp_data,
  pai_method = "lm",
  validation_type = "stratified",
  train_split_ratio = 0.75,
  n_strata = 4 # Use quartiles for stratification
)
print(stratified_assessment)
}

}
