% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_gcps.R
\name{st_as_sf.gcp}
\alias{st_as_sf.gcp}
\title{Convert a GCP Object to an sf Spatial Point Object}
\usage{
\method{st_as_sf}{gcp}(x, coords = c("source", "target"), crs = NULL, ...)
}
\arguments{
\item{x}{An object of class \code{gcp}.}

\item{coords}{A character string specifying which coordinates to use for
the spatial geometry: \code{"source"} (default) or \code{"target"}.}

\item{crs}{Optional Coordinate Reference System. If \code{NULL} (default), uses
the \code{crs} attribute of the \code{gcp} object if present.}

\item{...}{Additional arguments passed to \code{sf::st_as_sf()}.}
}
\value{
An \code{sf} spatial point object containing the GCP coordinates and displacements.
}
\description{
Converts a \code{gcp} object containing homologous control points into an \code{sf}
spatial point collection using either source or target coordinates.
}
\examples{
gcp_data <- read_gcp(
  source_x = c(10, 20, 30), source_y = c(15, 25, 35),
  target_x = c(12, 22, 32), target_y = c(17, 27, 37),
  crs = 3857
)
sf_pts <- sf::st_as_sf(gcp_data, coords = "source")
print(sf_pts)
}
