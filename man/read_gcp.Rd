% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_gcps.R
\name{read_gcp}
\alias{read_gcp}
\title{Read and Prepare Homologous Points (GCPs) to an \code{gcp} Object}
\usage{
read_gcp(source_x, source_y, target_x, target_y)
}
\arguments{
\item{source_x}{Numeric vector of approximate ('from') x coordinates.}

\item{source_y}{Numeric vector of approximate ('from') y coordinates.}

\item{target_x}{Numeric vector of actual ('to') x coordinates.}

\item{target_y}{Numeric vector of actual ('to') y coordinates.}
}
\value{
An \code{gcp} object of the homologous points and calculated \code{dx} and \code{dy}
displacement columns.
}
\description{
Imports data regarding homologous points and prepares them for
modeling by calculating displacement vectors and creating an \code{gcp} object.
}
\details{
This function is the first step in the modeling workflow. It
calculates the \code{dx} and \code{dy} errors that the models will learn to predict.
}
\examples{
# Sample data
source_coords <- data.frame(source_x = c(10, 20, 20, 30, 15),
                            source_y = c(15, 25, 15, 10, 5))
target_coords <- data.frame(target_x = c(12.5, 19.8, 19.6, 32.2, 14.0),
                            target_y = c(14.5, 27.2, 15.9, 9.8, 4.9))
# Read and prepare GCPs
gcp_data <- read_gcp(
  source_x = source_coords$source_x,
  source_y = source_coords$source_y,
  target_x = target_coords$target_x,
  target_y = target_coords$target_y
)
print(gcp_data)
summary(gcp_data)
plot(gcp_data)
}
