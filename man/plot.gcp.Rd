% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_gcps.R
\name{plot.gcp}
\alias{plot.gcp}
\title{Plot Method for gcp Objects}
\usage{
\method{plot}{gcp}(
  x,
  title = "Distortion Displacement Vectors",
  subtitle = "Arrows point from distorted to true locations",
  arrow_color = "darkred",
  point_color = "red",
  exaggeration_factor = 1,
  ...
)
}
\arguments{
\item{x}{An object of class \code{gcp}.}

\item{title}{A character string for the plot's main title.}

\item{subtitle}{A character string for the plot's subtitle.}

\item{arrow_color}{A character string specifying the color of the
displacement arrows.}

\item{point_color}{A character string specifying the color of the points
marking the source locations.}

\item{exaggeration_factor}{A numeric value to scale the length of the
displacement vectors. A value of 2, for instance, will double their
plotted length, making subtle displacements more visible. Defaults to 1
(no exaggeration).}

\item{...}{Additional arguments (not used).}
}
\value{
A \code{ggplot} object, which can be further customized using standard
\code{ggplot2} syntax.
}
\description{
Creates a visualization of the displacement vectors from source
(distorted) to target (true) coordinates for a set of homologous points.
}
\details{
This function is a key exploratory tool for understanding the nature
and magnitude of positional error in a dataset before correction. It plots
arrows that originate at the distorted \code{source} coordinates and point to
the correct \code{target} coordinates. This provides an immediate visual sense
of the spatial patterns in the distortion (e.g., rotation, scaling, or
non-linear warping).
}
\examples{
# Sample data
source_coords <- data.frame(source_x = c(10, 20, 20, 30, 15),
                            source_y = c(15, 25, 15, 10, 5))
target_coords <- data.frame(target_x = c(12.5, 19.8, 19.6, 32.2, 14.0),
                            target_y = c(14.5, 27.2, 15.9, 9.8, 4.9))
# Read and prepare GCPs
gcp_data <- read_gcp(
  source_x = source_coords$source_x,
  source_y = source_coords$source_y,
  target_x = target_coords$target_x,
  target_y = target_coords$target_y
)

# Create the default displacement plot
plot(gcp_data)

# Customize the plot with different titles and colors
plot(
  gcp_data,
  title = "My Custom Displacement Plot",
  subtitle = "Visualizing error vectors",
  arrow_color = "blue",
  point_color = "orange"
)

# Exaggerate the displacement vectors for clearer visualization
plot(gcp_data, exaggeration_factor = 5)
}
