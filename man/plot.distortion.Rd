% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analyze_distortion.R
\name{plot.distortion}
\alias{plot.distortion}
\title{Visualize a Distortion Metric as a Continuous Surface}
\usage{
\method{plot}{distortion}(
  x,
  metric = "area_scale",
  palette = "viridis",
  diverging = FALSE,
  value_range = NULL,
  add_points = TRUE,
  n_grid = 200
)
}
\arguments{
\item{x}{A \code{distortion} object returned by \code{analyze_distortion()}.}

\item{metric}{A character string specifying the metric to plot.}

\item{palette}{A viridis color palette name (e.g., "viridis", "magma").}

\item{diverging}{If \code{TRUE}, uses a red-white-blue diverging color scale.}

\item{value_range}{A numeric vector of length 2 specifying color scale limits.}

\item{add_points}{If \code{TRUE}, the original analysis points are overlaid.}

\item{n_grid}{The resolution of the interpolation grid (e.g., 200x200).}
}
\value{
A \code{ggplot} object.
}
\description{
Creates a smooth, interpolated surface visualization of a
distortion metric from the output of \code{analyze_distortion()}.
}
\details{
This function visualizes the distortion field as it exists on the
\strong{source map's coordinate space}. It uses linear interpolation via the
\code{akima} package to create a continuous raster surface, even from scattered,
irregular input points (like the original GCPs). This provides a true
surface plot in all cases.
}
\examples{
# See ?analyze_distortion for a runnable example.
}
