% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/models.R
\name{pai_model_list}
\alias{pai_model_list}
\title{Built-in PAI Transformation Model Registry}
\usage{
pai_model_list
}
\description{
Definitions of the transformation models shipped with \code{mapAI}.
}
\details{
Each entry of \code{pai_model_list} is a list describing one transformation model:
\describe{
\item{\code{label}}{Human-readable model name used in printed output.}
\item{\code{library}}{Name of the package required to fit the model, or \code{NULL}
for models that depend only on base R and \code{mapAI} itself. Checked with
\code{requireNamespace()} by \code{train_pai_model()}.}
\item{\code{modelType}}{Either \code{"univariate"} (the displacement components \code{dx}
and \code{dy} are fitted by two independent calls) or \code{"bivariate"} (a single
call receives the whole GCP data frame and returns both components).}
\item{\code{fit}}{For univariate models, \verb{function(x, y, ...)} where \code{x} is a data
frame of predictors and \code{y} the response vector. For bivariate models,
\verb{function(dat, ...)} where \code{dat} is the full GCP data frame, carrying
\code{source_x}, \code{source_y}, \code{target_x}, \code{target_y}, \code{dx} and \code{dy}.}
\item{\code{predict}}{\verb{function(modelFit, newdata, ...)}. Univariate models return
a numeric vector; bivariate models return a two-column object whose first
column is \code{dx} and second \code{dy}.}
}

This registry lives in package source rather than in \code{R/sysdata.rda} so that
the fitting and prediction mathematics is reviewable in a diff. The same
contract governs user-supplied custom models passed to \code{train_pai_model()} via
\code{method = list(...)}; see the \code{custom_model_training} vignette.
}
\keyword{internal}
